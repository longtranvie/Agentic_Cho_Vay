# Nhật ký phiên làm việc — 2026-06-08

Bản ghi tóm tắt công việc, quyết định và trạng thái của phiên (12 commit: `b7fe7f7` → `5ae6c10`).
58/58 test xanh. Mọi thứ đã commit + push lên `origin/main`.

---

## 1. Quyết định đã chốt (Nhóm A blocker)

| Mã | Quyết định | Hệ quả |
|---|---|---|
| **L1** | **Đường (I):** giữ OpenAI + tuân thủ cross-border đầy đủ | Kiến trúc LLM **giữ nguyên**. Phát sinh nghĩa vụ: consent, audit log, AI-transparency, báo sự cố 72h. Thủ tục nộp Bộ Công an + DPA là việc pháp chế Vapp |
| **L2** | **fintech** vay tiêu dùng | Áp **trần khoản vay 100tr** (TT12/2024) trong knock-out |
| **D1** | Dev cấp **bản nháp research** số quyết định | `decision_table.json` version `fintech-research-1` + `decision-table-rationale.md`. **CHƯA rủi ro Vapp duyệt**; trọng số scorecard cần dữ liệu thật (D5) |
| **D2** | Cập nhật corpus luật tới T6/2026 | VBHN 06/2026, NĐ356/2025, BLDS 2015, TT11/2021 |

> Cập nhật pháp lý quan trọng: **Nghị định 13/2023 đã hết hiệu lực 01/01/2026** → thay bằng **Luật 91/2025 + NĐ 356/2025**. Toàn bộ docs/ADR đã đồng bộ.

## 2. Công việc đã làm (theo commit)

| Commit | Nội dung |
|---|---|
| `56ac921` | Chế độ **chat tương tác offline** (nhân viên ảo hỏi từng field) |
| `47834a9` | RAG corpus cập nhật luật 2026 (bỏ TT39 gốc cũ) |
| `2442d7f` | **Báo cáo tiến độ** cho lãnh đạo (`bao-cao-tien-do.md`) |
| `d2febb5` | Đồng bộ tham chiếu pháp lý (NĐ13 → 91/2025 + 356/2025) |
| `b8f34c6` | **Sửa bug truy vấn RAG** + in trích dẫn chính sách ra CLI |
| `772a70c` | Gitignore CA bundle |
| `3046243` | **Siết hội đồng cho LLM thật** (enum + lọc cờ rác + prompt giàu dữ liệu) |
| `023d990` | L1/L2/D1: quyết định + bộ số research |
| `ef68328` | D2: mở rộng corpus luật chung |
| `46b2300` | **Tuân thủ NĐ356**: audit log + thông báo minh bạch xử lý tự động |
| `fdb6c05` | Ghi chú 3 biến CA cho OpenAI+Chroma sau proxy |
| `5ae6c10` | **Skeleton API** FastAPI (`/health`, `/assess`) + tách `service.py` |

## 3. Verify với hạ tầng thật

- **Proxy MITM gỡ được** bằng CA bundle (`corp-ca-bundle.pem` xuất từ kho chứng chỉ Windows). Cần **3 biến**: `LOAN_CA_BUNDLE`, `SSL_CERT_FILE`, `REQUESTS_CA_BUNDLE`.
- **OpenAI LLM:** verify thật — hồ sơ tốt → approve, recommendation enum hợp lệ, cờ sạch.
- **Chroma RAG (embedding text-embedding-3-large):** verify thật — semantic bắt được paraphrase ("mua vàng miếng" → Điều 8).
- **Audit log:** sự kiện `cross_border_transfer` chỉ ghi khi THỰC SỰ gửi OpenAI.

## 4. Trạng thái hiện tại

✅ Lõi quyết định tất định · pipeline LangGraph + hội đồng (siết cho LLM thật) · OpenAI + Chroma verify · audit + minh bạch NĐ356 · skeleton API · 58 test.

## 5. Còn lại để production

- 🔴 **Vành ngoài:** Postgres (nối phiên/TTL) · deploy Docker/AWS · auth API.
- 🔴 **Hội thoại nhiều lượt qua API** (cần Postgres).
- 🟡 **D1 chờ rủi ro Vapp ký** · **eval golden set (D5)** · **tuning RAG** (rerank/hybrid).
- 🟡 **Nghĩa vụ L1 còn lại:** consent flow onboarding · quy trình báo sự cố 72h (đi cùng API).
- 🟡 **Phụ thuộc Vapp:** hợp đồng API (T1) · region/VPC AWS (T2) → cần trước khi khóa interface.

## 6. Cách chạy

```powershell
# Offline demo (mock + keyword)
$env:PYTHONPATH="src"; $env:LOAN_LLM_PROVIDER="mock"; $env:LOAN_RAG_BACKEND="keyword"
python -m loan_agent.cli data/sample_applications.json   # batch
python -m loan_agent.cli                                 # hội thoại
python tests/run.py                                      # 58 test

# API skeleton
uvicorn loan_agent.api.app:app --reload                  # /docs để xem API

# Với OpenAI + Chroma thật (sau proxy): đặt 3 biến CA + LOAN_LLM_PROVIDER=openai (xem .env.example)
```
